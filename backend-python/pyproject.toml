[project]
name = "trading-bot-python"
version = "0.1.0"
description = "Scraper and AI engine layer for the trading bot platform."
requires-python = ">=3.11"

[tool.ruff]
line-length = 100

[tool.pytest.ini_options]
pythonpath = ["."]
